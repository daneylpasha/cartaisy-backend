# Agent instructions

## Pull request descriptions

If the task refers to a GitHub issue number, the PR description must include a GitHub closing keyword for that issue.

Example:

```text
Closes #12
```

Use the exact issue number from the task. This is required so GitHub automatically closes the issue when the PR is merged.
